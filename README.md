# SortLab

Interactive sorting visualizer built with React + TypeScript + Tailwind + Vite.

Install:

```bash
npm install
npm run dev
```

Features: random array, size/speed sliders, multiple algorithms, stats, sounds, pseudocode.
